export const OG_IMAGE_SIZE = { width: 1200, height: 630 } as const;

/** Matches playlist/card thumbnails (`aspectRatio: '16 / 9'`). */
export const CARD_ASPECT_RATIO = 16 / 9;

/** Width reserved for the featured image column in OG layouts. */
export const OG_FEATURED_IMAGE_WIDTH = 480;
